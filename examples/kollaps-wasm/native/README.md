# Running kollaps-wasm experiments with native programs
The topology file imitates the iperf3 example for swarm/docker orchestrator deployments.
It requires the `iperf3` executable to be available in PATH.
