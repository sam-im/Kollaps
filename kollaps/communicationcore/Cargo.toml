[package]
name = "communicationcore"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
capnp = "0.21"
capnp-schemas = { path = "../capnp-schemas" }
pyo3 = { version = "0.25.1", features = ["extension-module"] }

[lib]
name = "communicationcore"
crate-type = ["cdylib"]
